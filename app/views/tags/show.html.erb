<h2><%= @tag.name %></h2>

<% if @tag.posts.any? %>

  <h3>Posts:</h3>
  <ul>
    <% @tag.posts.each do |post| %>
      <li><%= link_to post.title, post_path(post) %></li>
    <% end %>
  </ul>

<% else %>
  <p>
    There aren't any posts with this tag yet.
  </p>
<% end %>
